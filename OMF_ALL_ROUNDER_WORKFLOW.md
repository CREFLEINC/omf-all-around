# OMF ALL ROUNDER — 파이널 루틴 인계

목적: **클라이언트 동작을 정본으로 서버와 빠르게 정합을 맞추고, 실제로 동작하는 프로그램을 완성한다.** 검증 비용은 일반적인 개발 수준으로 제한한다.

이 문서는 새 PC·새 세션의 시작 안내다. 사용자 최신 지시가 우선하며, 이전 문서의 “모바일·POP는 API 검증만 수행” 규칙은 아래 절차로 대체한다. 공통 워크플로와 검토된 하네스 복사본은 통합 관리 저장소에서 공유한다. 설치된 하네스는 클라이언트·서버 제품 PR에서 제외하며, 비밀정보·DB·테스트 증거는 별도 보관한다. 별도 표시가 없는 문서 경로는 통합 관리 프로젝트 기준이다.

## 1. 사용자가 준비할 것

- **통합 관리·클라이언트·서버를 clone한다.** `portable-setup/README.md`에 따라 하네스를 적용한다. 아직 원격에 없는 제품 커밋은 먼저 제품 PR로 반영하거나 별도 Git bundle로 이전한다. 복원 묶음에 없는 로컬 자료가 필요하면 따로 보존한다. 복원 확인 전 기존 PC 자료를 삭제하지 않는다.
- 새 PC의 프로젝트 위치, 사용할 서버(로컬 또는 테스트 배포 서버), 이어 할 시나리오를 알려준다. 가능하면 세 프로젝트를 같은 부모 폴더 아래 배치한다.
- 계정·환경 변수 등 비밀값은 안전하게 별도 전달한다. 문서·Git·일반 로그에 넣지 않는다. 같은 실행을 이어갈 때는 전용 시나리오 DB 백업도 이전한다.
- Docker 실행, OS 설치·접근 권한, 필요한 로그인, USB 디버깅 허용 등 직접 승인이 필요한 항목을 처리한다. 실기기 테스트 시 Android 단말기를 연결한다.
- 업무 목적·필수 조건·완료 기준을 정하고 최종 결과를 검토한다. 자동 조작이 막히면 에이전트가 지정한 화면 동작만 직접 수행한다.

## 2. 새 PC에서 에이전트가 할 것

1. 먼저 `docs/integration-protocol.md`와 각 저장소 지침을 읽고 **읽기 전용으로** 경로·Git 상태·로컬 하네스·기존 실행 프로세스를 확인한다. 기존 하네스를 무조건 초기화하지 말고 기술 지식은 유지하면서 충돌하는 운영 규칙만 정리한다.
2. 아래 인계 커밋과 로컬 파일이 복원됐는지 확인한다. 원격을 fetch하고 차이를 확인하되, 로컬 커밋·미커밋 파일을 보존한다. `origin/main`으로 강제 reset하지 않는다.
3. 클라이언트·서버 담당은 각각 실제 프로젝트 디렉토리에서 유지한다. 새 담당 세션 생성은 사용자 요청에 따라 수행하고, 확인한 ID만 `docs/session-registry.md`에 기록한다. 이전 PC의 세션 ID·PID·절대 경로를 재사용하지 않는다.
4. 저장소의 package/lockfile/CI를 기준으로 Node·패키지 관리자·Docker·JDK·Android SDK를 확인하고 의존성을 설치한다. 이전 PC의 `node_modules`와 빌드 캐시는 재사용하지 않는다. 가능한 설치·설정은 직접 처리하고 사용자 조작이 꼭 필요한 부분만 요청한다.
5. 서버 주소·포트·환경 변수를 새 PC에 맞춘다. 기존 프로세스를 확인한 뒤 서버·관리자 웹·POP를 실행하고 필요하면 모바일을 빌드·설치한다. **체크아웃된 코드와 실행 중인 바이너리가 같은 버전인지** 확인한다.
6. DB 연결 대상을 확인한다. 새 시나리오이면 전용 로컬 DB를 백업·초기화하고 마이그레이션 → 서버 `deploy/seed/` 기초 데이터 → 시나리오 준비 데이터를 적용한다. 이어 하는 실행이면 기존 DB와 생성 ID를 보존한다.
7. 건강 상태뿐 아니라 로그인·권한·단말 등록·공정/설비 매핑·필요 데이터 조회까지 확인한다. 부족한 준비 데이터를 직접 구성하고, 준비 결과와 남은 장애만 간단히 보고한다.

도구 권한을 미리 일괄 허용했다고 간주하지 않는다. 실제 승인 차단은 대상과 이유를 설명하며, 독립적으로 가능한 작업은 계속한다.

## 3. 시나리오 실행 절차

1. **계획:** 사용자 조건을 화면·액션 순서로 구체화한다. 입력값 출처, 생성 ID/LOT/QR, 수량, 이전·이후 상태와 다음 단계에서 사용할 값을 정의한다. 불명확한 업무 판단은 질문한다.
2. **준비:** 새 시나리오마다 검증된 전용 로컬 DB를 초기 상태로 만들고 필요한 계정·권한·마스터·시작 재고를 준비한다. 검증할 업무 결과 자체를 미리 넣어 통과시키지 않는다. E2E용 DB는 별도로 둔다.
3. **코드 검증 선행:** 모바일·POP에 구현된 API 클라이언트와 응답 처리 함수를 가능한 한 재사용해 순서대로 호출한다. 요청·인증·기대 응답뿐 아니라 앱의 해석·저장·다음 화면 조건도 확인한다. 설계 스펙만 보고 별도 요청을 만들어 대체하지 않는다. 대체한 네이티브 기능은 명시한다.
4. **수정:** 발견한 문제의 정의·원인·해결 방법을 정리해 담당에게 배정한다. 제품과 관련 회귀 테스트를 수정하고 영향 구간부터 재검증한다. 구현 완료 통보만으로 통합 통과 처리하지 않는다.
5. **직접 실행:** 코드 검증 후 관리자 웹·모바일·POP를 실제로 조작한다. 현재 테스트에서는 카메라 촬영을 제외하고 실제 발급 코드의 직접 입력/붙여넣기를 사용한다. API·화면 표시·재조회 결과를 연결해 확인한다.
6. **막힘 처리:** 조작 도구가 막히면 새 화면 상태를 확인해 제한적으로 재시도하고 빠르게 사용자 조작으로 전환한다. 원인을 특정하지 못한 실기기 문제는 최대 30분 또는 수정·재검증 2회 내에서 중단·보고한다. 사용자의 더 짧은 한도가 있으면 우선한다. 무응답 대기나 같은 실패를 반복하지 않는다.
7. **최종 검증:** 수정 후 전체 시나리오를 한 번 다시 실행한다. 최종 후보 코드에서 양쪽 저장소의 기존 필수 검사를 실행한다. 같은 코드에서 이미 통과한 검사는 근거 없이 반복하지 않는다. 실패·미실행은 그대로 표시한다.
8. **보고·반영:** 시나리오와 코드 검증 결과, 총 이슈 수와 해결/미해결/별건 수, 잔여 제한, 증거 위치, Git 상태를 보고한다. 커밋·push·PR·병합은 사용자가 요청한 범위까지 진행하며 각 상태를 구분한다. 다음 시나리오는 사용자와 정한다.

중간 승인을 반복해서 받지 않는다. 맡긴 작업은 완료 통보 또는 상태 조회로 확인하고 다음 조치를 이어 간다. 정기 예약 점검을 임의로 만들지 않는다. 필수 검증이 남아 있으면 전체 완료로 판정하지 않는다.

## 4. 시행착오에서 정리한 실행 기준

| 항목 | 적용 기준 |
|---|---|
| 실제 단말기 통신 | 단말기의 `localhost`는 Mac/PC가 아니다. 접근 가능한 LAN 주소 또는 명시적으로 설정한 `adb reverse`를 사용한다. 서버 바인딩·방화벽·API 주소부터 확인한다. |
| 모바일 빌드 | API 주소는 빌드 결과에 포함되므로 대상 주소와 `/api`를 확인한다. HTTP이면 필요한 대상 호스트의 cleartext 설정과 네이티브 HTTP 설정을 확인한다. `JAVA_HOME` 이름만 믿지 말고 실제 JDK 21인지 실행해서 확인한다. |
| 단말 등록 | 서버 변경·DB 초기화·코드 재발급 후 이전 토큰을 그대로 쓰지 않는다. 등록 완료 API/상태를 확인한다. 온라인·RUNNING은 등록 완료와 별개다. 신규 단말은 미등록이며 생성 시 상태를 선택받지 않는다. |
| 인증 | 관리자 웹의 쿠키와 모바일·POP의 Bearer 인증을 구분한다. 토큰은 해당 서버에서 발급된 것을 사용하고 비밀 헤더를 로그에 남기지 않는다. |
| 작업 진입 | 사번·권한·공정/설비 매핑·재고·필수 선택값을 확인한다. 빈 목록이나 비활성 버튼을 곧바로 API 결함으로 단정하지 않는다. |
| QR·라벨 | 실제 발급된 내용을 사용한다. 제품 UI는 QR 촬영이 기본, 직접 입력은 보조다. QR 생성·이미지 표시·실제 프린터 출력은 각각 별도 판정한다. |
| 자동 조작 | 클릭이 불확실하면 재조회부터 한다. 등록·확정·입고 같은 쓰기 동작을 무작정 반복하지 않는다. 사용자 조작 후에도 결과 검증은 통합 담당이 맡는다. |
| 동시 작업 | 같은 DB를 변경하는 테스트 실행자는 한 명만 둔다. 직접 시연 중에는 다른 담당이 서버를 재시작하거나 코드를 교체하지 않는다. |
| 증거 | 첫 실행과 수정 완료 후 최종 전체 실행에만 단계별 스크린샷을 보존한다. 중간 재검증은 실패 구간 로그 중심이다. 첫 실행 전체 통과 시 추가 전체 실행은 없다. 증거는 `evidence/<시나리오>/<실행>/`에 보관한다. |
| 계약·커밋 | 서버 `contracts/*.json`은 읽기 전용 설계 사본이다. 선행 변경은 서버 `docs/계약-선행-수정항목.md`에 즉시 P-항목으로 기록하고 제품 PR에 포함한다. 하네스는 `docs/local-harness-files.json`과 staged diff로 제외한다. `git add .`/`git add -A`는 쓰지 않는다. |

모델 배정은 `docs/model-routing.md`를 따른다. 시나리오·복잡한 판단은 Astra, 일반 구현은 Terra, 정형 실행은 Luna를 기본으로 실제 지원 모델을 명시한다. 단순 작업에 고비용 모델을 일괄 사용하지 않는다.

## 5. 이번 이전 시점의 기준 — 2026-09-14

아래는 로컬 확인 시점의 값이다. 새 PC에서 반드시 다시 비교한다. 원격 상태를 새로 fetch한 결과라는 의미는 아니다.

| 저장소 | 로컬 main HEAD | 로컬 origin/main 참조 대비 |
|---|---|---|
| `omf-mes-client` | `041fa44f` | 3개 커밋 앞섬, 뒤처짐 없음 |
| `omf-mes-server` | `7862f4e` | 2개 커밋 앞섬, 뒤처짐 없음 |

- 양쪽 모두 로컬 하네스 등 미커밋·미추적 파일이 남아 있다. **전체 변경을 일괄 커밋하거나 없애지 말고 이전한다.** 제품 변경 내역은 `evidence/FR-008/client/commit-sync-report.md`, `evidence/FR-008/server/commit-sync-report.md`를 참고한다.
- 직전 FR-008 출고·수신 시나리오: 3품목 100/50/10 출고, 출고측 잔량 0/50/90, 수신측 재고 100/50/10 확인. 직접 실행 근거는 `evidence/FR-008/ui-replay-01/result.md`에 있다. 실제 프린터 출력은 별도 미해결이며 업무·코드 검사 전체 결과와 혼동하지 않는다.
- 최신 테스트용 모바일 APK는 `deliverables/mobile-test-mesapi/`에 있다. 대상은 **`http://mesapi.crefle.ai/api`**이며 Samsung SM-S928N에 설치했다. 설치 완료는 해당 배포 서버의 업무 테스트 완료를 뜻하지 않는다. 세부 정보는 `evidence/mobile-test-mesapi/build-report.md`를 참고한다.
- 로컬 환경 이전은 실행 중인 서버나 연결된 단말 세션을 복제하는 작업이 아니다. 새 PC에서 다시 실행·연결하고 사용자가 로컬/배포 서버 중 어느 대상으로 테스트할지 확인한다.

## 새 세션에 전달할 시작 문구

> OMF_ALL_ROUNDER_WORKFLOW.md를 읽고 파이널 루틴 환경을 준비해줘. 먼저 portable-setup/README.md에 따라 하네스를 확인·적용하고, 이전된 제품 커밋·필요한 로컬 파일·DB와 도구 상태를 확인하고, 보존할 데이터를 훼손하지 말아줘. 사용자 직접 조작이나 불명확한 업무 결정이 필요한 항목만 질문하고 나머지는 스스로 준비해줘. 준비 결과에는 실행 대상 서버, 코드 버전, 사용 가능한 앱, 남은 장애를 구분해서 보고해줘.
